Name="Application Audio Output Capture"

Mode="Mode"
Mode.Window="Capture specific window"
Mode.Hotkey="Capture foreground window with hotkey"

Window="Window"
Window.Priority="Window Match Priority"
Window.Priority.Title="Window title must match"
Window.Priority.Class="Match title, otherwise find window of same type"
Window.Priority.Exe="Match title, otherwise find window of same executable"

Hotkey.Start="Capture foreground window"
Hotkey.Stop="Deactivate capture"

ExcludeProcessTree="Capture all audio EXCEPT the selected window (if the window exists)"